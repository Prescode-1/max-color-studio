{
  "version": 2,
  "framework": null,
  "installCommand": "npm --prefix frontend install",
  "buildCommand": "NITRO_PRESET=vercel npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "env": {
    "NITRO_PRESET": "vercel"
  }
}
