{
  "name": "max-color-studio-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && NITRO_PRESET=vercel npm --prefix frontend run build"
  }
}
